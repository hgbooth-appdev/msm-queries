
    
    <h1>
  Actor #<%= @curAct.id%> details
</h1>

<a href="/actors" class="btn btn-block btn-outline-secondary">
  Go back
</a>

&nbsp;|&nbsp;

<a href="/delete_actor/192" class="btn btn-block btn-outline-secondary">
  Delete actor
  <small>(Building this link is not required. It's an optional stretch goal; but, you have the tools if you want to give it a try.)</small>
</a>

<hr>

<dl>
  <dt>
    Name
  </dt>
  <dd>
    <%= @curAct.name%>
  </dd>

  <dt>
    Dob
  </dt>
  <dd>
    <%= @curAct.dob%>
  </dd>

  <dt>
    Bio
  </dt>
  <dd>
    <%= @curAct.bio%>  
    </dd>

  <dt>
    Image
  </dt>
  <dd>
    <img src="<%= @curAct.image%>" style="width:250px;">
  </dd>

  <dt>
    Created at
  </dt>
  <dd>
    <%= time_ago_in_words(@curAct.created_at) %>
  </dd>

  <dt>
    Updated at
  </dt>
  <dd>
    <%= time_ago_in_words(@curAct.created_at) %>
  </dd>
</dl>

<hr>

<h2>Filmography</h2>

<table border="1">
  <tbody><tr>
    <th>
      Title
    </th>

    <th>
      Year
    </th>

    <th>
      Director
    </th>

    <th>
      Character name
    </th>

    <th>
    </th>
  </tr>

<% @chars.each do |c|%>

<% mov = Movie.where({:id => c.movie_id}).first %>
<% dir = Director.where({:id => mov.director_id}).first %>

<tr>
  <td><%= mov.title %></td>
  <td><%= mov.year %></td>
  <td><%= dir.name %></td>
  <td><%= c.name %></td>
  <td><a href="/movies/<%= mov.id %>">Show details</a></td>
</tr>


<% end %>
</tbody></table>
